# Handoff: Agent Workforce — Day 2 完成，进入数据积累阶段

## 快速恢复

下次进入时说：
> "读一下 ~/agent-workforce/handoffs/2026-03-27-day2.md，继续 Agent Workforce"

## 当前状态

系统核心链路已跑通，累计 243 条 trace，完成 3 轮 profile 迭代。**当前最大问题是反馈覆盖率 0%**。下一步应停止加功能，正常工作积累数据。

## 三个核心指标 (NORTH_STAR)

| 指标 | 目标 | 当前 |
|------|------|------|
| 反馈覆盖率 | >50% | 0% |
| 任务完成率 | >80% | 34% (旧数据拖低) |
| Post-Edit Ratio | <15% | 未采集 |

## 必读文件

进入项目后按此顺序读：
1. `constitution.md` — 长期原则
2. `AGENTS.md` — 工作规则
3. `docs/NORTH_STAR.md` — 核心指标
4. `docs/ARCHITECTURE_RECOMMENDATION.md` — Codex 架构建议

## Immediate Next Steps

1. 确认 `.zshrc` 里 claude() 函数已加 `_aw_post_claude` 调用（终端评分提示）
2. 正常做其他项目开发，让 trace 自然积累
3. 积累到 50 条有反馈的 trace 后，跑 `python3 evolution/nightly_eval.py --engine claude`

## 关键架构决策

- **Solo Run 为默认**，不做 agent 网络
- **Proposal-first**，不自动改 profile
- **Dashboard 走 Caddy /aw/**，不依赖 9100 端口
- **改 profiles/server 后必须跑 `bash scripts/sync_to_server.sh`**
- **改核心三文件 (trace_engine/session_stop/feishu_notify) 前必须有 test case**

## 已完成的迭代 (5 lessons + 2 rules)

| 来源 | Lesson |
|------|--------|
| trace 热力图 | infra: 飞书模板先测试再改调用 |
| completion 分析 | web: 改动后必须 build |
| retry 分析 | 全局: 模糊目标先澄清 |
| 多文件对照 | backend: 多文件先列改动计划 |
| VPN case study | 5 条规则: 完整验证/记录 why/画拓扑/先调研/批量人机交互 |

## 服务端

| 服务 | 地址 | 管理 |
|------|------|------|
| Dashboard | http://118.196.147.14/aw/ | Caddy 反代 → systemd aw-server |
| Profiles | http://118.196.147.14/aw/profiles | 同上 |
| Feedback API | http://118.196.147.14/aw/api/feedback?trace_id=xxx&rating=3 | 同上 |
| GitHub | https://github.com/housenyou123/agent-workforce | public |

## 常用命令

```bash
aws                                    # 系统状态
awt                                    # 今天 traces
bash scripts/sync_to_server.sh         # 同步到服务器
python3 scripts/inject_profiles.py     # 注入 profiles
python3 evolution/nightly_eval.py --engine claude  # 夜间评测
```

## Gotchas

- `.zshrc` 和 `settings.json` 编辑权限被拒，需用户手动改
- Docker 覆盖 iptables → 9100 端口反复挂 → 已用 Caddy /aw/ 绕过
- `_PROJECT_ROUTES` 有缓存，改 config.yaml 后需重启 Python
